@model  DatabaseInteraction.Models.Assignment
@{
    Layout = "";
}

<script>
  
</script>

<div class="modal-content animate">
    @using (Ajax.BeginForm("EditGoalAssignment", "Student", new AjaxOptions { HttpMethod = "POST", OnSuccess = "assignmentUpdated" }))
    {
        <div class="modal-header">
            <!-- Dynamically insert students name -->
            <h4 class="modal-title">Edit Assignment</h4>
        </div>
        @Html.EditorFor(m => m)
    }

</div>

